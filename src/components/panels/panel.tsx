import { styled } from '@panda/jsx';

const Panel = styled('section', {
    base: {
        position: 'relative',
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        width: '100vw',
    },
});

export default Panel;
