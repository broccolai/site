import { Eyebrow, HeaderArea, HeaderText, HeaderTitle } from '@/features/tier-list/components/styles';

const TierListPageHeader = () => (
    <HeaderArea>
        <HeaderText>
            <Eyebrow>BR-02 RELAY / RANKING SHEET</Eyebrow>
            <HeaderTitle>Tier List</HeaderTitle>
        </HeaderText>
    </HeaderArea>
);

export default TierListPageHeader;
