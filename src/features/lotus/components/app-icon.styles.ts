import { css } from '@panda/css';
export const iconFrame = css({
    display: 'inline-flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexShrink: '0',
    fontSize: '0.9375rem',
    fontWeight: '600',
    lineHeight: '1',
});
export const iconImage = css({ display: 'block', width: '100%', height: '100%', objectFit: 'contain' });
